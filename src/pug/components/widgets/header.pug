mixin header(props = {})

  header.header#header(class=props.mod && `header--${props.mod}`)
    .width

      .header__core
        nav
          each item in dataMenu || []
            a(
              href=item.href
              class=item.href === currentUrl && 'is-active'
            )!=item.title
